import { FilterEventsBasedOnTitlePipe } from './filter-events-based-on-title.pipe';

describe('FilterEventsBasedOnTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEventsBasedOnTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
